import 'package:flutter/material.dart';

class ProjectConfigurationFrame extends StatelessWidget {
  const ProjectConfigurationFrame({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}