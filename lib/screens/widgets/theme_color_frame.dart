import 'package:flutter/material.dart';

class ThemeColorFrame extends StatelessWidget {
  const ThemeColorFrame({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}